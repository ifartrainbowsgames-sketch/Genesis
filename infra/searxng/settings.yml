use_default_settings: true

general:
  debug: false
  instance_name: "Genesis Search"

search:
  safe_search: 1
  autocomplete: ""
  formats:
    - html
    - json

server:
  secret_key: "genesis-local-change-me"
  limiter: false
  image_proxy: false
